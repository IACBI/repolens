# Docs Edge

See [the guide](docs/guide.md).

Use `src/index.ts:1` as the first source file.

Run `pnpm install` and `pnpm test`.

These stale examples should be reported:

```bash
pnpm missing
npm start
```

The removed file was `<src/missing.ts:12>`.
