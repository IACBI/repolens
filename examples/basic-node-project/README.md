# Basic Node Project

This is a small example repository shape that RepoLens can scan.

It contains:

- a `package.json` with common scripts
- a TypeScript source entry point
- a short README

From the RepoLens project root, you can inspect the example by running RepoLens with a config that includes `examples`, or by copying this directory into a separate test workspace.
